import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'DivisApp_1120031',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
